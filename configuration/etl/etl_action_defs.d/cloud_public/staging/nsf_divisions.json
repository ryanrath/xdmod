{
    "#": "This action loads data from a structured file and only needs the table definition",
    "table_definition": {
        "$ref": "${table_definition_dir}/cloud_public/staging/nsf_divisions.json#/table_definition"
    }
}
